<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yzx.system.mapper.SysPostMapper">

    <resultMap type="com.yzx.model.system.SysPost" id="SysPostResult">
        <id     property="postId"     column="post_id"     />
        <result property="postCode"   column="post_code"   />
        <result property="postName"   column="post_name"   />
        <result property="postSort"   column="post_sort"   />
        <result property="status"     column="status"      />
        <result property="remark"     column="remark"      />
        <result property="tenantId"   column="tenant_id"   />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <sql id="selectPostVo">
        select post_id, post_code, post_name, post_sort, status, remark, tenant_id, create_time, update_time
        from sys_post
    </sql>

    <select id="selectPostList" parameterType="com.yzx.model.system.SysPost" resultMap="SysPostResult">
        <include refid="selectPostVo"/>
        <where>
            <if test="postCode != null and postCode != ''">
                AND post_code like concat('%', #{postCode}, '%')
            </if>
            <if test="postName != null and postName != ''">
                AND post_name like concat('%', #{postName}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        order by post_sort asc
    </select>

    <select id="selectPostAll" resultMap="SysPostResult">
        <include refid="selectPostVo"/>
        order by post_sort asc
    </select>

    <select id="selectPostById" parameterType="Long" resultMap="SysPostResult">
        <include refid="selectPostVo"/>
        where post_id = #{postId}
    </select>

    <select id="selectPostsByUserId" parameterType="Long" resultMap="SysPostResult">
        select p.post_id, p.post_code, p.post_name, p.post_sort, p.status, p.remark, p.tenant_id, p.create_time, p.update_time
        from sys_post p
            left join sys_user_post up on p.post_id = up.post_id
            left join sys_user u on up.user_id = u.user_id
        where u.user_id = #{userId}
        order by p.post_sort asc
    </select>

    <select id="selectPostListByUserId" resultType="Long">
        select p.post_id
        from sys_post p
            left join sys_user_post up on p.post_id = up.post_id
            left join sys_user u on up.user_id = u.user_id
        where u.user_id = #{userId}
        order by p.post_sort asc
    </select>

    <select id="checkPostNameUnique" parameterType="String" resultMap="SysPostResult">
        <include refid="selectPostVo"/>
        where post_name = #{postName} limit 1
    </select>

    <select id="checkPostCodeUnique" parameterType="String" resultMap="SysPostResult">
        <include refid="selectPostVo"/>
        where post_code = #{postCode} limit 1
    </select>

    <select id="countPostByDeptId" resultType="int">
        select count(1) from sys_user_post up
        inner join sys_user u on up.user_id = u.user_id and u.dept_id = #{deptId}
    </select>

    <delete id="deletePostById" parameterType="Long">
        delete from sys_post where post_id = #{postId}
    </delete>

    <delete id="deletePostByIds" parameterType="Long">
        delete from sys_post where post_id in
        <foreach collection="array" item="postId" open="(" separator="," close=")">
            #{postId}
        </foreach>
    </delete>

</mapper>
